.env
# .env.example is deliberately TRACKED — an untracked template helps nobody.
node_modules/
.qodo

# build output
dist/
*.tsbuildinfo

# logs (D-03: winston file transports removed in favour of stdout)
*.log
